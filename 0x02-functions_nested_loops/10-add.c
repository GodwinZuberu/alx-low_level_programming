#include "main.h"
#include <stdio.h>

/**
* main - entry point
*
* @add - prints addition of two numbers
* @i: holds the value of i
* @k: holds the value of k
* Return: Always 0.
*/

int add(int i, int k)
{
	return (i + k);
}
