#include "main.h"
#include <stdio.h>

/**
* main - entry point
*
* description - Write a function that adds two integers and returns the result.
* Return: Always 0.
*/

int add(int i, int k)
{
	return (i + k);
}
